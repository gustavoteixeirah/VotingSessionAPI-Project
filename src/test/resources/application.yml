spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/voting_session

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: basic